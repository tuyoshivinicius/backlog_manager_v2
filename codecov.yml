coverage:
  status:
    project:
      default:
        target: 80%
    patch:
      default:
        target: 80%

ignore:
  - "tests/"
  - "specs/"
  - ".specify/"

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
